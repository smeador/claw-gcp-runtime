{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-lab.local/openclaw.runtime-secrets.schema.json",
  "title": "OpenClaw Runtime Secrets Payload",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "auth": {
      "type": "object",
      "description": "Sensitive OpenClaw provider or integration authentication config merged into the runtime config."
    },
    "channels": {
      "type": "object",
      "description": "Sensitive channel configuration such as bot tokens merged into the runtime config."
    },
    "hooks": {
      "type": "object",
      "description": "Hook configuration and any hook-scoped secret values merged into runtime hooks."
    },
    "gateway": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "auth": {
          "type": "object",
          "description": "Sensitive gateway authentication config merged into gateway.auth."
        }
      },
      "required": [
        "auth"
      ]
    }
  },
  "required": [
    "auth",
    "gateway"
  ]
}
