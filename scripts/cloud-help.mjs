#!/usr/bin/env node

const lines = [
  "Unified runtime facade",
  "",
  "  agent-runtime cloud COMMAND",
  "    Preferred in-repo runtime CLI when direnv is enabled.",
  "",
  "Dependency commands",
  "",
  "  npm run deps:show",
  "    Show pinned repo dependency versions and detected local tool versions.",
  "",
  "  npm run deps:check",
  "    Check latest available versions for auto-managed dependency entries without editing versions.json.",
  "",
  "  npm run deps:bump",
  "    Update auto-managed entries in versions.json to their latest available versions.",
  "",
  "  npm run deps:sync",
  "    Render derived dependency files such as config/docker.build.env and the Cloud Function package manifest.",
  "",
  "  npm run deps:lock:function",
  "    Refresh the Cloud Function package-lock.json from the pinned versions.",
  "",
  "Cloud commands",
  "",
  "These commands use operator defaults from env vars or direnv:",
  "  VM_NAME, PROJECT_ID, ZONE, OPENCLAW_SECRET_NAME",
  "",
  "  agent-runtime cloud help",
  "    Print this command list.",
  "",
  "  agent-runtime cloud push-secret",
  "    Upload config/secrets.cloud.json (or CLOUD_SECRET_FILE) to Secret Manager.",
  "",
  "  agent-runtime cloud sync",
  "    Sync the reviewed app tree to the VM without deploying.",
  "",
  "  agent-runtime cloud deploy",
  "    Sync app files, install host prerequisites, render runtime artifacts, and start the cloud gateway.",
  "",
  "  agent-runtime cloud restart",
  "    Re-render runtime artifacts and restart the cloud gateway without rebuilding the image.",
  "",
  "  agent-runtime cloud rebuild",
  "    Sync app files, force a clean no-cache image rebuild, and recreate the cloud gateway.",
  "",
  "  agent-runtime cloud ps",
  "    Show cloud Docker container status on the VM.",
  "",
  "  agent-runtime cloud logs",
  "    Show openclaw-gateway logs from the cloud VM.",
  "",
  "  agent-runtime cloud agent-logs",
  "    Show the latest cloud agent activity stream and runtime log from the gateway container.",
  "",
  "  agent-runtime cloud logs-download",
  "    Download the cloud session log bundle locally using the current direnv cloud settings.",
  "",
  "  agent-runtime cloud shell",
  "    Open an interactive shell inside the cloud gateway container.",
  "",
  "  agent-runtime cloud tunnel",
  "    Open a local tunnel to the remote cloud gateway at http://127.0.0.1:18789/overview.",
  "",
  "  agent-runtime cloud cron apply",
  "    Reconcile repo-managed cloud cron jobs into the gateway without creating duplicates.",
  "",
  "  agent-runtime cloud cron list",
  "    List installed cloud cron jobs.",
  "",
  "  agent-runtime cloud cron run-digest",
  "    Run the morning digest cron job immediately for debugging.",
  "",
  "  agent-runtime cloud test gmail-read",
  "    Run a Gmail read/search test inside the cloud gateway container.",
  "",
  "  agent-runtime cloud test gmail-send",
  "    Send a Gmail test message from pip@meador.me from the cloud gateway container.",
  "",
  "  agent-runtime cloud test digest",
  "    Run the Pip newsletter digest from the cloud gateway container.",
  "",
  "  agent-runtime cloud test skill SKILL_NAME",
  "    Run a skill-owned cloud test entrypoint through the generic runtime dispatcher.",
  "",
  "Runtime test tiers currently exist only for local Docker:",
  "  agent-runtime local test basic|core|integration",
  "",
  "Optional overrides:",
  "  CLOUD_SECRET_FILE   Alternate cloud secret file path",
  "  CLOUD_CRON_FILE     Alternate cloud cron config path",
  "  TAIL_LINES          Number of log lines for agent-runtime cloud logs",
  "  AGENT_NAME          Agent name for agent-runtime cloud agent-logs (default: main)",
  "  LOG_VIEW            agent-runtime cloud agent-logs view: messages (default), replies, errors, or full",
  "  SHOW_RUNTIME_LOG    Set to 1 to append the raw runtime log tail to agent-runtime cloud agent-logs",
  "  GMAIL_TEST_TO       Recipient for agent-runtime cloud test gmail-send",
  "  GMAIL_TEST_SUBJECT  Subject for agent-runtime cloud test gmail-send",
  "  SKILL_TEST_MESSAGE  Generic message override for agent-runtime cloud test skill",
  "  SKILL_TEST_TIMEOUT_MS  Generic timeout override for agent-runtime cloud test skill",
  "  DIGEST_MESSAGE      Custom agent prompt for agent-runtime cloud test digest",
  "  DIGEST_TEST_TIMEOUT_MS  Timeout override for agent-runtime cloud test skill pip-newsletter-digest"
];

console.log(lines.join("\n"));
